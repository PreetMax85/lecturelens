# Frontend (frontend/.env)
# Set this to your deployed backend URL once you deploy (e.g. Render URL).
# Leave unset during local dev - the Vite dev proxy handles /chat locally.
VITE_API_URL=https://your-backend.onrender.com